#!/usr/bin/env ruby

require 'todo_api_cli'

if ARGV.size != 3
  puts 'Creating users requires name, email, password'

  exit(1)
end

TodoApi::Cli::Users.create(name: ARGV[0], email:  ARGV[1], password:  ARGV[2])